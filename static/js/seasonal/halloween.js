/**
 * halloween theme — placeholder.
 *
 * Phase 0 scaffolding: proves the mount/teardown contract without drawing
 * anything. Replaced with the real engine in a later phase.
 */

export const mount = () => ({
  destroy() {}
});
